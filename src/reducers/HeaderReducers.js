import imgPhone from "./../img/phone.png"

let initialState={
    imgPhone:{img:imgPhone}
}

export const HeaderReducers = (state=initialState, action) => {
    switch(action.type){
        default:return state
    }
}